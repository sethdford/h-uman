# Build artifacts
build/
build-*/

# Build output logs
build.log
build-*-output.txt
test-output.txt
*.o
*.so
*.dylib
*.exe
*.a
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json
.cache/

# Benchmark output (transient)
benchmark-results.json

# Runtime data
*.db
*.db-journal

# Contact/message data (privacy: never commit raw contact lists or message data)
data/*.contacts.json
scripts/contacts*.json
scripts/message-analysis*.json

# Python
__pycache__/
*.pyc

# Editor / OS
.DS_Store
*.swp
*~
*.bak

# Environment
.env
.env.*
!.env.example

# Nix
result
.direnv/

# Archive (local-only Zig reference)
archive/
reference/

# MCP config (local build paths)
.mcp.json

# Claude Code local settings and session artifacts (not shared)
.claude/settings.local.json
.claude/daemon-config.json
.claude/*-audit.md
.claude/*AUDIT*.md

# Worktrees
.worktrees
.claude/worktrees/

# iOS / macOS / Swift build caches
apps/ios/.build/
apps/ios/.swiftpm/
apps/macos/.build/
apps/macos/.swiftpm/
apps/macos/Human.xcodeproj/
apps/shared/.build/
apps/shared/HumanKit/.build/

# Android build caches
apps/android/.gradle/
apps/android/build/
apps/android/app/build/
apps/android/local.properties

# Node / frontend
node_modules/
ui/dist/
ui/node_modules/
ui/test-results/
ui/playwright-report/
ui/blob-report/
# Root-level Playwright artifact directory — appears when tests are run
# from repo root (e.g. via `cd ui && npx playwright test` then a tool
# auto-cds back). Already covered for ui/, but not root. PR #113 round
# 25 caught a stray test-results/.last-run.json commit (Bugbot 150b4dcb).
/test-results/
/playwright-report/
ui/visual-regression/
ui/coverage/
ui/e2e-screenshots/

# E2E test artifacts (screenshots, ad-hoc scripts)
ui/e2e-*.png
ui/e2e-*.mjs
ui/e2e-final-browser/
ui/e2e-part1/
ui/e2e-part2/
ui/e2e-final-fixes/
ui/e2e-targeted/
ui/e2e-definitive/
ui/e2e-proof/
ui/e2e-final-tool/
ui/e2e-focused/
ui/e2e-definitive-final/
ui/e2e-absolute-final/
ui/e2e-final-final/
ui/e2e-pressure/
ui/e2e-*
.audit_hmac_key
audit_tool_test.log
audit.log

build2/
build_clean/
audit.log
firebase-debug.log
human_test_output.txt
human_test_stderr.txt
eval_results.json
eval_baselines.json
eval_blinded_results.json
eval_success_criteria.json
# grounding_ab.py per-trial output contains real message text (PII) — never commit
data/grounding_ab.json

# Training data and model weights
convo-training-*/
training-data/
debug/
data/imessage/
data/facebook/
data/merged/
*.safetensors
src/data/data_*_safetensors.c
src/data/data_imessage_*.c
src/data/data_eval_blinded_ab_json.c
src/data/data_eval_golden_set_jsonl.c
src/data/data_eval_results_json.c
.lighthouseci/
debug
convo-training/
lora-persona-seth.bin

# Private corpus + derivatives (per RL-loop spec §13)
# Note: git treats `~` as a literal char, not home-dir expansion.
# `**/.human/private/` matches the per-user corpus wherever it appears
# inside the working tree.
.human/private/
**/.human/private/

# Sprint 11 / US-11.6 D1 fixture policy: production-tier YNTP holdout
# lives only on Seth's machine at ~/.human/private/yntp_holdout_30.jsonl
# and must NEVER appear in the repo, no matter what directory it's in.
yntp_holdout_30.jsonl
**/yntp_holdout_30.jsonl

# Phase 1 (RL SOTA) — Gemma GGUF and other large model files. The
# fetcher script writes to ~/.human/models/, but contributors who
# fetch from inside the repo for testing should never accidentally
# commit a 4 GB GGUF.
.human/models/
**/.human/models/
*.gguf

# Phase 6 E2E proof artifacts (generated by tests/demo)
tests/_tmp/proofs/
adapters/

