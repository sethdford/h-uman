# Better than human — program plan (2026-09-06)

Owner session: 089d4814. Successor to `~/.claude` memory `project_h-uman_sota_next_2026-09-04`.
Definition of "better than human": detection ≤ 0.35 on the blind human gate (n≥40) AND
specificity per reply ≥ Seth's own AND prospective-memory F1 > 0.65 AND timing within rater
preference. Today only the first is measured (0.225, n=40, 2026-07-27).

Each item ships behind its gate; no item is DONE on a green suite alone.

| # | Item | Gate (measurement) | Status |
|---|------|--------------------|--------|
| 1 | Identity invariants: `hu_persona_creator_write` round-trips unknown keys + atomic write; doctor check flags a persona that lost keys vs its newest backup | test: write-then-load preserves an unknown key; doctor errors on the 2026-09-06 gutted file, ok on the restored one | DONE (writer 3/3, doctor 7/7, live doctor ok) |
| 2 | State-first memory read path (graph_state.c resolver built 09-04): honor event_end/supersedes_id end-to-end | held-out changed-facts set retrieved at the right version | DONE (resolver + composer deployed 09-05; held-out gate 6797ac465: 24 shuffled chains × 3 moments, 12/12). Residual: one relation per neighbor entity in the composer dedupe |
| 3 | Insight stream + budget-reserved recall block in the compact head (survives the 16 KB MLX cap) | named entities / insider refs per reply vs Seth; LUAR twin not dropping; EI flat | todo |
| 4 | Sleep-time consolidation into a linted Seth-wiki feeding the head (uses GPU hours freed by the reflection fix) | prompt bytes down, composite flat/up | BUILT 2026-09-13, gated OFF: `scripts/consolidate_wiki.py` (deterministic v1, provenance-tagged, linted; 11 pages live in `~/.human/wiki/`), `ai.human.wiki-nightly` 05:20 loaded, loader block behind `HU_WIKI_HEAD` (live = recall cap drops by page bytes). Next: deploy, plist `HU_WIKI_HEAD=shadow`, then the offline A/B |
| 5 | Prospective memory wired (table exists, reads empty) | deferred intentions executed on cue | FIXED 2026-09-13: it had never fired — nothing set `fired=1`, match was substring ("work" hit "works"), 3 oldest rows forever. Now whole-word, newest first, one row per intention, retired after one render, logged `[prospective] fired N of M`. Writer filters cues (stop list, >5% of the contact's texts, one cue per intention); prune retired 584/949 live rows (fired=2). Gate = `SELECT COUNT(*) FROM prospective_memories WHERE fired=1` after deploy, plus the log line |
| 6 | Close the parametric loop: adapters self-promote behind LUAR + Binoculars + Seth-judge; v6 human-rated | promotion happens without a hand install; doctor blind_ab_gate error clears | todo |
| 7 | Local-only: tool turns stay on GLM (no gemini degrade), proposer local, fallback = say less | zero Vertex calls on the reply path over 24h | todo |
| 8 | Human gate re-run n=40 after 2–4 | detection ≤ 0.35 held | todo |

Do NOT: another voice round, steering, Q6, reconstructive-recollection redesign before its harness key fix.

## Log
- 2026-09-06 08:xx — persona gutted by ungated style reanalyze; restored + gated (1a7f395ea). Program started.
- 2026-09-06 — item 1 landed: creator_write preserves unknown keys + atomic rename; doctor persona_integrity (newest-backup reference). Attribution: 06:01 writer was a partial-struct CLI write (analyzer shape), not reanalyze. Gemini 3.8 Flash verified 200 and set as fallback + proposer per Seth.
- 2026-09-06 — item 3 step A: prompt budget 16 KB → 24 KB. Re-measured GLM on :8741 with cache-defeating prompts, fact at 60% depth: 16/24/32/40 KB all answered + recalled, prompt_tokens linear (3031/4518/5947/7518), cold latency 10.2/15.0/20.6/27.5 s. Live prompt mean ~17 KB (persona 9.0K, pm 2.1K, guard 1.8K, memory 1.6K, stm 1.1K) so the old cap cut memory+pm most turns. Gate for step A: per-turn prompt_trim/truncation log shows 0 memory/pm bytes cut on ordinary turns after deploy.
- 2026-09-06 — item 3 step B: specificity baseline (scripts/specificity_score.py) human 2.13 vs daemon 1.10 specific tokens/reply (FAIL, expected). contact_insights repo + loader block (HU_INSIGHT_STREAM, default off) + scripts/insight_stream.py (local GLM, persona-conditioned). Dry run across 9 contacts produced dated, priced, named notes; wrote 70 live rows. Plist set to shadow; live promotion waits on the offline A/B (same inbound messages, with/without block) moving the specificity metric.
- 2026-09-06 — item 5: prospective_memories had readers (daemon_reactive_prompt.c keyword check) but NO writer on any live path, and the match was case-sensitive. The commitments ledger (143 pending, 0 followed up, newest rows are the CONTACT's phrases) is not a usable source. Added the extractor pass + case-fold fix + test. First offline A/B for insights: n=36, +0.03 specific tokens/reply, 10 improved / 6 worse / 20 tied — not enough for live; rerunning at ~100 with a grounding measure.
- 2026-09-06 — insight A/B n=108 (results/insight-ab-n100-20260906.json): specific tokens/reply 0.778→0.981 (+26%), share-with-any 49%→57%, grounded-in-thread 2.8%→15.7%, improved 31 / worse 14 / tied 63 (sign test p≈0.016). Promoted HU_INSIGHT_STREAM shadow→LIVE. Final arbiter remains the n=40 human gate (item 8). Item 4 design written (item4-sleep-consolidation.md). Item 6: rating drip for the served ORPO adapter is running (orpo1 sheet, 0/48 answered, question pending to Seth since 10:16) — clears the doctor blind_ab_gate error when answered.
- 2026-09-13 — audit (Seth: stay on GLM, focus on memory): item 5 had NEVER fired. 949 open triggers, 0 fired, and no code wrote `fired=1`; the SQL `instr()` match fired "work" (18 rows, 7 intentions of one contact) on "bath and body works"; 48 triggers had a qualifying inbound and none was observable. Fix: whole-word match in C, newest-first + one row per intention, `hu_prospective_mark_fired` after render, a `[prospective]` log line; extractor keyword filter + `--prune-triggers` (584 retired, fired=2). Item 3 note: live specificity is unreadable at ~7 replies/day (50 since live; 50-vs-50 window 0.80→0.78/reply) — the offline paired A/B stays the gate. Item 4 built as deterministic v1 (no model call; provenance by construction) — see item4-sleep-consolidation.md. Nightly extractor was already scheduled (05:05); wiki-nightly added at 05:20.
- 2026-09-19 — SOTA sweep (Jul–Sep papers, all verified) → `docs/plans/2026-09-19-beyond-sota.md`: per-item "beyond" plan. Items 4+5 DEPLOYED (PR #419). New: K-verified admission + evidence provenance + supersession for insights (this PR); DivEye variability features in the detector tier (PR #420). Served adapter unchanged since 09-05 (candidates staged, gate BLOCK) so the 09-18 anti-AI dip is not a retrain. Human gate still 0/48 answered — item 8 is blocked on Seth answering the drip.
