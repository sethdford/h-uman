/*
 * tests/test_frontier_prompt.c — placeholder.
 *
 * The frontier_prompt subsystem (`include/human/agent/frontier_prompt.h` and
 * `src/agent/frontier_prompt.c`) was deleted by a parallel agent on this
 * branch. This file is left as a compile-clean stub so the test runner
 * registry doesn't break; the original lifecycle tests will be restored
 * (or re-homed elsewhere) when the subsystem lands in its new shape.
 */

#include "test_framework.h"

void run_frontier_prompt_tests(void) {
    HU_TEST_SUITE("frontier_prompt");
    /* Intentionally empty — tracked in the bundle that removed
     * src/agent/frontier_prompt.c. */
}
